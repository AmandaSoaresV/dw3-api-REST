### Rotas da API de Contatos

- `GET /contatos`  
  mostra todos os contatos

- `GET /contatos/:id`  
  busca pelo ID

- `POST /contatos`  
  cria um novo contato

- `PUT /contatos/:id`  
  atualiza  pelo ID

- `DELETE /contatos/:id`  
  remove 